# GitHub personal access token (optional, for issue/PR enrichment)
# Create one at https://github.com/settings/tokens
# Only needs public read access — no special scopes required
GITHUB_TOKEN=ghp_your_token_here
